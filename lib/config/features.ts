/**
 * 功能开关配置文件
 *
 * 用于控制各种付费/实验性功能的启用状态
 * 在发布时可以根据需要调整这些开关
 */

// ============================================================================
// 高清放大功能配置
// ============================================================================

/**
 * 是否启用分镜高清放大功能
 *
 * 启用后，分镜生成流程将变为：
 * 1. 生成 1080P 网格图
 * 2. 使用 fal.ai SeedVR2 放大到 2K/4K
 * 3. 切割放大后的高清图
 *
 * 注意：此功能会产生额外费用（fal.ai 按 megapixel 计费）
 *
 * @default false - 默认关闭，发布时根据需要启用
 */
export const ENABLE_STORYBOARD_UPSCALE = true;

/**
 * 分镜放大的目标分辨率
 *
 * - '2K': 放大到 2560x1440（适合大多数场景）
 * - '4K': 放大到 3840x2160（最高质量，费用更高）
 *
 * @default '2K'
 */
export const STORYBOARD_UPSCALE_RESOLUTION: '2K' | '4K' = '2K';

// 行级注释：FAL_API_KEY 在服务端 API 路由中从 process.env 读取，不在客户端暴露

// ============================================================================
// 其他功能开关（预留）
// ============================================================================

/**
 * 是否启用调试模式
 * 启用后会在控制台输出更多日志
 */
export const DEBUG_MODE = process.env.NODE_ENV === 'development';

